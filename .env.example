APP_PORT=3000
NODE_ENV=development

# ----------------------------------------------------
# DATABASE (MongoDB)
# ----------------------------------------------------
# Example: mongodb://user:password@localhost:27017/mydb?authSource=admin
DATABASE_URL=

# ----------------------------------------------------
# JWT Authentication
# ----------------------------------------------------
# Generate a secure random string for production (e.g., use a password generator)
# Example command to generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
